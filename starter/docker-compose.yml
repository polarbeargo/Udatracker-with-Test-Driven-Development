version: '3.9'

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: udatracker-backend
    ports:
      - "5001:5000"
    environment:
      PYTHONPATH: /app
    restart: unless-stopped
